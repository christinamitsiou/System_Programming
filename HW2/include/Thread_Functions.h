#include "Queue.h"
#include "header.h"
#include "JES_helping_functions.h"

// The thread functions used by JobExecutorServer



// A global queue struct representing the buffer
Queue* buffer;

// The main thread function
void* main_thread_func(void* arg);

// The worker thread function
void* worker_thread_func(void* arg);

// The controller thread function
void* controller_thread_func(void* arg);

// The function that redirectes a job's output to a file and then executes said job
void child_server(Job_Info* job_to_execute);


